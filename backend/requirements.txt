fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.4
motor==3.4.0
pymongo==4.7.2
redis==5.0.4
httpx==0.27.0
python-dotenv==1.0.1
python-multipart==0.0.9

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
mongomock==4.1.2
mongomock-motor==0.0.34
fakeredis==2.23.2
# mongomock depends on pkg_resources, which setuptools>=81 removed entirely.
# Pin below that line so `pip install -r requirements.txt` works on a fresh
# Python 3.12+ environment without a separate manual setuptools install.
setuptools<81
